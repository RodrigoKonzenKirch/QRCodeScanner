The QRCode Scanner application scans QR Code and shows the result on the screen.

- The interface is implemented using Jetpack Compose.
- The Scanner uses ML Kit from Google.
- It works offline and does not require camera permission.
